{
  "name": "ai-widget-factory",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prepare:dynamic": "node packages/icons/scripts/prepare-dynamic-icons.js",
    "build:icons": "node packages/icons/scripts/generate-icons.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
